#include <tunables/global>

profile scanner flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # Capabilities
  file,
  signal (send) set=(kill,term,int,hup,cont),

  # S6-Overlay
  /init ix,
  /bin/** ix,
  /usr/bin/** ix,
  /run/{s6,s6-rc*,service}/** ix,
  /package/** ix,
  /command/** ix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /run/{,**} rwk,
  /dev/tty rw,

  # Bashio
  /usr/lib/bashio/** ix,
  /tmp/** rwk,

  # Python runtime
  /usr/bin/python3 rix,
  /usr/bin/python* rix,

  # System libraries
  /lib/** mr,
  /usr/lib/** mr,
  /usr/local/lib/** mr,

  # Application files
  /opt/scanner/** r,

  # Data and configuration
  /data/** rw,
  /media/** rw,
  /share/** rw,

  # Device access for scanners
  /dev/bus/usb/** rw,
  /dev/usb/** rw,

  # Network access
  network inet dgram,
  network inet stream,

  # System files
  /etc/passwd r,
  /etc/group r,
  /etc/nsswitch.conf r,
  /etc/resolv.conf r,
  /etc/ssl/certs/** r,

  # Proc and sys
  @{PROC}/sys/kernel/random/uuid r,
  /sys/bus/ r,
  /sys/class/ r,
  /sys/devices/** r,

  # Temporary files
  /tmp/** rw,
  /var/tmp/** rw,

  # SANE configuration
  /etc/sane.d/** r,
  /usr/share/sane/** r,

  # Avahi daemon configuration
  /usr/sbin/avahi-daemon ix,
  /etc/avahi/** r,
  /var/run/avahi-daemon/** rw,
  /var/lib/avahi/** rw,

  # Network discovery and mDNS
  network netlink raw,
  network inet dgram,
  network inet6 dgram,

  # DBus access for avahi
  /usr/bin/dbus-daemon ix,
  /var/run/dbus/** rw,
  /etc/dbus-1/** r,

  # Service dependencies and runtime
  /etc/services.d/**/dependencies.d/** rw,
  /run/s6/** rw,
  /run/s6-rc/** rw,

  # Process and signal management
  capability setuid,
  capability setgid,
  capability sys_admin,
  capability dac_override,

  # Network broadcast for mDNS
  network inet dgram,
  network inet6 dgram,
  network netlink raw,

  # Unix domain sockets for IPC
  unix,

  # Service runtime directories
  /run/avahi-daemon/** rw,
  /run/dbus/** rw,

  # System runtime
  /run/systemd/** r,
  /run/udev/** r,
}